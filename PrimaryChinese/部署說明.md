# Portal 部署說明 | Portal Deployment Instructions

## 🚀 快速部署

### 方法 1: 使用部署腳本（推薦）

在 PowerShell 中執行：

```powershell
cd "c:\Users\keith\OneDrive\Desktop\Profile"
.\DEPLOY.ps1
```

### 方法 2: 手動執行 Git 命令

```powershell
# 1. 進入項目目錄
cd "c:\Users\keith\OneDrive\Desktop\Profile"

# 2. 檢查狀態
git status

# 3. 添加所有檔案
git add -A

# 4. 提交變更
git commit -m "更新所有 portal 並重新上傳所有檔案"

# 5. 推送到 GitHub
git push origin main
```

## ✅ 所有 Portal 列表

1. **語文大富翁** (`chinesemonopoly/index.html`)
2. **語文百萬富翁** (`millionaire/index.html`) - 配置了 GitHub Actions
3. **修辭防衛戰** (`Rhetoric_Defense/index.html`)
4. **故事骰子生成器** (`story_dice/index.html`)
5. **錯別字手術室** (`typo_surgeon/index.html`)
6. **虛詞大搜查** (`ac_particle_fill_in/index.html`)
7. **成語動物園競賽** (`idiom-zoo-race/index.html`)

## 🔧 故障排除

### 如果沒有遠程倉庫配置：

```powershell
git remote add origin https://github.com/你的用戶名/你的倉庫名.git
```

### 如果分支名稱不同：

```powershell
git branch -M main
```

### 如果推送被拒絕：

```powershell
git push -u origin main --force
```

（注意：使用 --force 會覆蓋遠程倉庫的內容，請謹慎使用）

---

*更新日期: 2025-01-27*
