# 🚀 GitHub 部署步驟

## 快速部署指南

### 步驟 1: 準備 Git 倉庫

如果還沒有初始化 Git：

```bash
cd PrimaryChinese/語文百萬富翁
git init
git add .
git commit -m "Initial commit: 語文百萬富翁遊戲"
```

### 步驟 2: 推送到 GitHub

```bash
# 如果還沒有遠程倉庫
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 步驟 3: 啟用 GitHub Pages

1. 進入您的 GitHub 倉庫頁面
2. 點擊 **Settings**（設置）標籤
3. 在左側菜單找到 **Pages**（頁面）
4. 在 **Source** 下拉菜單中選擇 **GitHub Actions**
5. 點擊 **Save**（保存）

### 步驟 4: 等待自動部署

- GitHub Actions 會自動開始構建和部署
- 可以在 **Actions** 標籤頁查看部署進度
- 通常需要 2-5 分鐘完成

### 步驟 5: 訪問您的網站

部署完成後，您的網站地址將是：

```
https://YOUR_USERNAME.github.io/Profile/PrimaryChinese/語文百萬富翁/
```

或者如果是在根倉庫：

```
https://YOUR_USERNAME.github.io/語文百萬富翁/
```

## ⚙️ 調整 Base Path

如果您的 GitHub Pages URL 路徑不同，需要修改 `vite.config.js`：

### 情況 1: 在子目錄中（如 Profile/PrimaryChinese/語文百萬富翁/）

```javascript
base: '/Profile/PrimaryChinese/語文百萬富翁/'
```

### 情況 2: 在倉庫根目錄

```javascript
base: '/'
```

### 情況 3: 倉庫名就是域名（如 username.github.io）

```javascript
base: '/語文百萬富翁/'
```

修改後需要：
1. 重新構建：`npm run build`
2. 提交更改：`git add . && git commit -m "Update base path" && git push`

## 🔧 手動部署（可選）

如果不想使用 GitHub Actions，可以手動部署：

```bash
# 安裝 gh-pages
npm install --save-dev gh-pages

# 部署
npm run deploy
```

## ✅ 驗證部署

部署成功後，您應該能夠：

1. ✅ 看到遊戲主頁面
2. ✅ 點擊「開始遊戲」按鈕
3. ✅ 看到 3D 場景正常顯示
4. ✅ 所有功能正常運作

## 🐛 常見問題

### 問題：頁面顯示 404

**解決方案：**
- 檢查 `vite.config.js` 中的 `base` 路徑是否正確
- 確保 GitHub Pages 設置正確
- 等待幾分鐘讓 GitHub 更新

### 問題：資源加載失敗

**解決方案：**
- 檢查瀏覽器控制台的錯誤信息
- 確保所有路徑都是相對路徑
- 清除瀏覽器緩存

### 問題：GitHub Actions 失敗

**解決方案：**
- 檢查 `.github/workflows/deploy.yml` 文件是否存在
- 查看 Actions 標籤頁的錯誤日誌
- 確保 Node.js 版本兼容（項目使用 Node 18）

## 📝 更新內容

每次更新代碼後：

```bash
git add .
git commit -m "更新內容描述"
git push
```

GitHub Actions 會自動重新部署！

## 🎉 完成！

現在您的遊戲已經在 GitHub Pages 上運行了！

