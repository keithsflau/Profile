name: Deploy Idiom Asteroids to GitHub Pages

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'PrimaryChinese/idiom_asteroids/**'
      - '.github/workflows/deploy-idiom-asteroids.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages-idiom-asteroids"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: '**/package-lock.json'
      
      - name: Install dependencies
        working-directory: PrimaryChinese/idiom_asteroids
        run: npm install
      
      - name: Build
        working-directory: PrimaryChinese/idiom_asteroids
        run: npm run build
      
      - name: Setup Pages
        uses: actions/configure-pages@v6
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: 'PrimaryChinese/idiom_asteroids/dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5

